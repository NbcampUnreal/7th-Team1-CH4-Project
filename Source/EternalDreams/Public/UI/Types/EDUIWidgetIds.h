#pragma once

#include "CoreMinimal.h"

namespace EDUIWidgetIds
{
	// 메인 HUD 레이아웃 ID
	static const FName HUD_Main(TEXT("HUD.Main"));

	// 인벤토리 패널 ID
	static const FName Panel_Inventory(TEXT("Panel.Inventory"));

	// 일시정지 패널 ID
	static const FName Panel_PauseMenu(TEXT("Panel.PauseMenu"));

	// Player Status 위젯 ID
	static const FName HUD_PlayerStatus(TEXT("HUD.PlayerStatus"));

	// Skill Bar 위젯 ID
	static const FName HUD_SkillBar(TEXT("HUD.SkillBar"));

	// 상호작용 프롬프트 위젯 ID
	static const FName HUD_InteractionPrompt(TEXT("HUD.InteractionPrompt"));
}